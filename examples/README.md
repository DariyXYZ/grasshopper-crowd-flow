# Examples

The primary public demo lives in:

- `flow-demo/Flow.gh`
- `flow-demo/Flow.3dm`

Open the Rhino scene first, then the Grasshopper definition.

The goal of this folder is to let a new user try the plugin immediately after build and deploy, without preparing their own test scene first.
